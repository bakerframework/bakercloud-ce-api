Since many people tend to get tripped up when dealing with .htaccess files (since they are generally hidden by the OS).  When installing the MagRocket API you need to rename the following file that exists in the root of the installation.

RENAME

htaccess.txt

TO

.htaccess

If you don't do this, the API won't work.

